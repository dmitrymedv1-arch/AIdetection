# =====================================
# Основное приложение и файлы
# =====================================
streamlit>=1.55.0
python-docx>=1.1.2

# NLP — основной стек (важно держать совместимыми)
spacy>=3.8.0
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

transformers>=5.2.0
sentence-transformers>=5.2.3
torch>=2.10.0                  # или конкретно 2.10.0
accelerate>=0.35.0             # под новые transformers

# Данные + метрики
numpy>=1.26.4,<2.0             # пока без numpy 2.0, чтобы не ломать старый код
pandas>=2.2.2
scipy>=1.13.0

# Визуализация (streamlit + plotly — отличная связка)
plotly>=5.22.0
matplotlib>=3.9.0              # опционально, если нужен seaborn или сложные стат. графики

# Утилиты
tqdm>=4.66.0
python-dotenv>=1.0.1           # если используешь .env
