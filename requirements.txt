# =====================================
# Основное приложение и файлы
# =====================================
streamlit
python-docx

# NLP — основной стек (важно держать совместимыми)
spacy==3.7.5
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
pydantic==1.10.14
transformers
sentence-transformers
torch                  # или конкретно 2.10.0
accelerate             # под новые transformers

# Данные + метрики
numpy             # пока без numpy 2.0, чтобы не ломать старый код
pandas
scipy

# Визуализация (streamlit + plotly — отличная связка)
plotly
matplotlib              # опционально, если нужен seaborn или сложные стат. графики

# Утилиты
tqdm
python-dotenv           # если используешь .env


